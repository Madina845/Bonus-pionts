#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    bool found = false;
    for (int i = ceil(sqrt(a)); i <= floor(sqrt(b)); i++) {
        cout << i * i << " ";
        found = true;
    }

    if (!found) {
        return 0;
    }

    return 0;
}
