#include <iostream>
#include <cctype>  // Для isdigit
using namespace std;

int main() {
    char c;
    cin >> c;
    
    // Проверяем, является ли символ цифрой
    if (isdigit(c)) {
        cout << "yes" << endl;
    } else {
        cout << "no" << endl;
    }
    
    return 0;
}
