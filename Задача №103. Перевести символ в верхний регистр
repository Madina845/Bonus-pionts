#include <iostream>
#include <cctype>  // Для функции toupper
using namespace std;

int main() {
    char c;
    cin >> c;
    
    cout << static_cast<unsigned char>(toupper(c)) << endl;
    
    return 0;
}
