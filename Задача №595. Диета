#include <iostream>
#include <vector>
#include <iomanip>  // для точности вывода
using namespace std;

int main() {
    double X, Y, Z;
    int N;
    
    // Ввод необходимых количеств белков, жиров и углеводов
    cin >> X >> Y >> Z;
    
    // Ввод количества продуктов
    cin >> N;
    
    // Инициализация сумм белков, жиров и углеводов
    double totalProteins = 0.0, totalFats = 0.0, totalCarbs = 0.0;
    
    // Ввод данных о каждом продукте и подсчет общей суммы питательных веществ
    for (int i = 0; i < N; ++i) {
        double A, B, C, Q;
        cin >> A >> B >> C >> Q;
        totalProteins += A * Q;
        totalFats += B * Q;
        totalCarbs += C * Q;
    }
    
    // Проверка, достаточно ли питательных веществ
    if (totalProteins >= X && totalFats >= Y && totalCarbs >= Z) {
        cout << "YES" << endl;
    } else {
        cout << "NO" << endl;
    }
    
    return 0;
}
