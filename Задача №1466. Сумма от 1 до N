#include <iostream>
using namespace std;

int main() {
    long long N;
    cin >> N;

    if (N > 0) {
        cout << N * (N + 1) / 2 << endl;
    } 
    else {
        cout << (N * (N - 1)) / 2 + 1 << endl;
    }

    return 0;
}
